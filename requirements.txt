# ============================================================
# EEEM068 — Applied Machine Learning
# Action Recognition Using Vision Transformers
# University of Surrey | 2025/6
# ============================================================
# Install all dependencies:
#   pip install -r requirements.txt
#
# GPU Required: NVIDIA A100 / T4 (Google Colab recommended)
# Python version: 3.10+
# ============================================================

# ── Core Deep Learning ───────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ── HuggingFace — Models (TimeSFormer + VideoMAE) ────────────
transformers==4.40.0
accelerate>=0.20.0
huggingface-hub>=0.16.0

# ── Vision Transformer Utilities ─────────────────────────────
timm>=0.9.2
einops>=0.6.1

# ── Data Science & Machine Learning ──────────────────────────
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0

# ── Image & Video Processing ─────────────────────────────────
Pillow>=9.4.0
opencv-python>=4.7.0
av>=10.0.0

# ── Visualisation ────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Logging & Monitoring ─────────────────────────────────────
tensorboard>=2.12.0

# ── Gradio Demo (Part E Live App) ────────────────────────────
gradio>=3.35.0

# ── Utilities ────────────────────────────────────────────────
tqdm>=4.65.0

# ============================================================
# NOTES:
#
# 1. Google Colab installation (recommended):
#    !pip install -r requirements.txt
#
# 2. For Google Colab — Google Drive mount is also needed:
#    from google.colab import drive
#    drive.mount('/content/drive')
#
# 3. PyTorch with CUDA (for local GPU setup):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 4. PyTorch 2.6+ users — fix for weights_only error:
#    Use: torch.load(..., weights_only=False)
#
# 5. Models are downloaded automatically from HuggingFace:
#    TimeSFormer: facebook/timesformer-base-finetuned-k400
#    VideoMAE:    MCG-NJU/videomae-base-finetuned-kinetics
# ============================================================
